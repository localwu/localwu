(function(){const copyMailAddrBtn=document.getElementById('copy-mail-addr-btn');const emailElement=document.getElementById('copied-mail-addr');if(!copyMailAddrBtn||!emailElement)return;const originalText=copyMailAddrBtn.textContent;copyMailAddrBtn.addEventListener('click',async function(e){e.preventDefault();const email=emailElement.textContent;try{await navigator.clipboard.writeText(email);copyMailAddrBtn.textContent='已复制';copyMailAddrBtn.classList.add('success');setTimeout(()=>{copyMailAddrBtn.textContent=originalText;copyMailAddrBtn.classList.remove('success')},2000)}catch(err){copyMailAddrBtn.textContent='复制失败';copyMailAddrBtn.classList.add('error');setTimeout(()=>{copyMailAddrBtn.textContent=originalText;copyMailAddrBtn.classList.remove('error')},2000)}})})();